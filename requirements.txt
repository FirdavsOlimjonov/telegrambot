# ─── Telegram Bot Framework ───────────────────────────────────────────────────
aiogram==3.13.1
aiogram-dialog==2.2.0           # FSM-based dialog windows (optional, powerful)

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36              # Async ORM (use 2.x — massive async improvements)
asyncpg==0.30.0                 # Async PostgreSQL driver (fastest available)
alembic==1.14.0                 # Schema migrations

# ─── Config & Validation ──────────────────────────────────────────────────────
pydantic==2.10.3                # Data validation / DTOs
pydantic-settings==2.6.1        # .env → Settings model
python-dotenv==1.0.1            # .env loader fallback

# ─── Excel Import ─────────────────────────────────────────────────────────────
pandas==2.2.3
openpyxl==3.1.5                 # xlsx engine for pandas
xlrd==2.0.1                     # xls engine (legacy support)

# ─── Utilities ────────────────────────────────────────────────────────────────
loguru==0.7.3                   # Structured logging (replaces stdlib logging)
aiofiles==24.1.0                # Async file I/O (Excel upload handling)
python-magic==0.4.27            # MIME type validation for uploaded files

# ─── Dev / Testing ────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.28.1                   # Async HTTP client (for webhook testing)
factory-boy==3.3.1              # Test fixtures
